{"actor":"codex-ravel","entity":"ravel-p2","kind":"create","op":"20260727T024323.917576Z-p31732-c0016-r6789-h07844c","set":{"body":"# View algebra\n\nAuthority: PRD.json 0.1.0, release target 1.0.\n\n## Objective\nImplement and fuzz the complete 1.0 structural view contract.\n\n## Product requirements\n\n## RAV-VIEW-001 [must]\nRavel shall provide composable axis-local structural operations with explicit storage behavior.\n\nAcceptance:\n- select, slice, narrow, reverse, swapAxes, permuteAxes, newAxis, squeeze, and broadcastTo return views.\n- reverse represents reversal with a negative stride.\n- broadcastTo returns a read-only zero-stride view where expansion is required.\n- contiguous returns the same value when already canonical contiguous or an owned contiguous copy otherwise.\n- copy and flattenCopy always copy.\n- reshapeView never copies.\n\n## RAV-VIEW-002 [must]\nSlice(start, stopExclusive, step) shall be the canonical slicing value, with Scala Range accepted only as convenience sugar.\n\nAcceptance:\n- Slice rejects step equal to zero.\n- Positive slices use boundaries in [0, axisLength].\n- Negative slices use start in [0, axisLength) and stopExclusive in [-1, axisLength).\n- Slice(axisLength - 1, -1, -1) performs a complete reversal.\n- Empty slices are valid.\n- Ravel does not reinterpret negative Slice endpoints as indices from the end.\n- Inclusive Scala Range conversion adjusts its endpoint with checked Long arithmetic.\n\n## RAV-VIEW-003 [must]\nAxis-taking APIs shall accept and normalize negative axes consistently.\n\nAcceptance:\n- Existing axes accept values in [-rank, rank).\n- newAxis accepts insertion positions from -(rank + 1) through rank.\n- permuteAxes normalizes every entry and rejects duplicates or omissions.\n- Element indices remain nonnegative in 1.0.\n\n## RAV-VIEW-004 [must]\nreshapeView shall succeed only when logical row-major traversal can be repartitioned without changing its physical address sequence.\n\nAcceptance:\n- Source and target sizes agree using checked Long arithmetic.\n- Length-one source axes are ignored for coalescing.\n- A non-singleton zero-stride source axis rejects reshapeView.\n- Adjacent non-singleton source axes coalesce exactly when outerStride equals innerShape times innerStride.\n- Target non-singleton axes can be partitioned in logical order into groups whose products equal source contiguous-block products.\n- Derived strides are checked before narrowing to Int.\n- An equal-size zero-element target may use a canonical empty layout when all target strides are representable.\n- Failure reports NonContiguousLayout and performs no copy.\n\n## Deliverables\n- select, Slice, Range conversion, narrow, reverse, swapAxes, permuteAxes, newAxis, squeeze, reshapeView, contiguous, and copy.\n- Negative-axis normalization.\n- Documented reshape block-coalescing algorithm.\n\n## Exit criteria\n- Random view compositions agree with the reference model.\n- Fuzzing covers zero-length, negative-stride, singleton zero-stride, and rejected-overflow layouts.\n- Every operation's view or copy behavior matches the public table.\n\n## Required law coverage\nViewLaws over contiguous, transposed, sliced, reversed, zero-length, singleton zero-stride, and composed layouts.\n\n## Scope constraint\nDo not add NumPy's heterogeneous selector grammar in 1.0. The typed axis-local operations remain canonical.\n\n## Completion rule\nClose this epic only when every listed requirement and exit criterion has executable evidence on every promised platform. Record implementation commits, tests, compiler-output evidence, and any accepted deviation in mote notes.","priority":1,"status":"open","title":"EPIC P2: Implement Ravel's structural view algebra"},"ts":"2026-07-27T02:43:23.917576Z","v":1}